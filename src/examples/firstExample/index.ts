import { ExampleProject } from "../types";
import { files } from "./fileStructure";

// @todo remove for prod
const firstEverExampleProject: ExampleProject = {
    name: "First Ever Example Project",
    directory: files,
};
export default firstEverExampleProject;
